ARG NGINX_PORT_HTTP
ARG NGINX_PORT_HTTPS
ARG NGINX_IMAGE_VERSION

FROM nginx:${NGINX_IMAGE_VERSION}

LABEL author=aiden_fe@outlook.com

COPY ./config /etc/nginx

EXPOSE ${NGINX_PORT_HTTP}
EXPOSE ${NGINX_PORT_HTTPS}
